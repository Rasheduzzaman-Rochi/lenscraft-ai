# Frontend

Home for the Next.js application using TypeScript and Tailwind CSS.

Future work includes the application UI, reusable components, styling, and frontend
tests. Dependency manifests and framework configuration will be added when the
application is initialized.

Only public configuration belongs in `NEXT_PUBLIC_` variables. Database credentials
and privileged Supabase keys must remain on the backend.
